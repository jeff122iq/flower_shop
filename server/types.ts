export interface IInfoRows {
    rows: [
        {
            id: string;
            title: string;
            description: string;
        }
    ]
}

export interface IUserRows {
    rows: [
        {
            id: string;
            username: string;
            email: string;
            password: string;
        }
    ]
}